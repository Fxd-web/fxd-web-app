export default {
  user: (state) => state.user.user,
  config: (state) => state.config,
  customerAuthInfo: state => state.information.customerAuthInfo,
  local_product: state => state.product.local_product,
}
